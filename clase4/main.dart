import 'package:flutter/material.dart';

void main() {
  runApp(const FormularioApp());
}

class FormularioApp extends StatelessWigdet (
)
}

@override
Widget build(BuildContext context){
	return Scaffold{
		appBar: AppBar()
		title: Text:("Calculo de RFC"),
	},
	body: Padding (
		padding: EgdeInserts.all(10,0),
		child: Column (
			children: [
				Text("Nombre")
			],
		),
	
	
}
